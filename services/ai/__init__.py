"""AI agent services — thin wrappers around agentic-core recruitment agents.

Feature flags control whether LLM-backed agents run. When disabled,
endpoints return deterministic-only results with no LLM call.
"""
